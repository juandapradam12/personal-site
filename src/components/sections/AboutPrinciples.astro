---
import SectionHeading from '../ui/SectionHeading.astro';
import { getAboutConfig } from '../../lib/content';

const about = getAboutConfig();
const hasPrinciples = about.principles.length > 0;
---

{
  hasPrinciples && (
    <SectionHeading title="How I work" id="how-i-work">
      {about.principlesIntro && (
        <p class="mb-4 text-sm" style="color: var(--color-muted);">
          {about.principlesIntro}
        </p>
      )}
      <ol class="space-y-3">
        {about.principles.map((step, index) => (
          <li class="flex items-start gap-3">
            <span
              class="mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full text-xs font-semibold"
              style="background: var(--color-accent-soft); color: var(--color-accent);"
            >
              {index + 1}
            </span>
            <div>
              <p class="text-sm font-semibold">{step.title}</p>
              {step.description && (
                <p class="text-sm" style="color: var(--color-muted);">
                  {step.description}
                </p>
              )}
            </div>
          </li>
        ))}
      </ol>
    </SectionHeading>
  )
}
